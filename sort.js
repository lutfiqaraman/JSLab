/*
        That will do sorting in a new array, while keep the original array as it is
        without change it
 */

const items = ["car", "wheels", "motor", "bike"];
const sortedItems =  [...items].sort();

console.log(items);
console.log(sortedItems);
